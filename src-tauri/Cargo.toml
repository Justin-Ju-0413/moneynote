[package]
name = "moneynote"
version = "1.6.0"
description = "MoneyNote - 本地优先的智能记账"
edition = "2021"
rust-version = "1.77.2"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-single-instance = "2"

[profile.release]
strip = true
lto = "thin"
codegen-units = 1
